export default class LifecycleNode {
	constructor(id, label) {
		this.id = id
		this.label = label
		this.x = 0 // default x-position
		this.y = 0 // default y-position
		this.radius = 10 // default radius
		this.color = "#ccc" // default color
	}
}
